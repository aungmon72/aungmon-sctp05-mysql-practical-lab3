<!--  5.1 Creating the route to view all the customers
-->

{{#extends "base"}}
{{#block "content"}}
<h1>Customers</h1>
<table class="table">
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Rating</th>
        <th>Company</th>
        
        <!-- 5.6 Add a link to update 
        -->
        <th>Action</th>
    </tr>


    <!--5.1 Creating the route to view all the customers
        Use a #each helper to render one row per customer.
    -->

    {{#each customers}}
    
    <tr>
        
        <td>customers.first_name
            {{customers.first_name}}
            {{customers.first_name}}
            {{customers.last_name}}
        </td>
        <td>{{customers.last_name}}</td>
        <td>{{customers.rating}}</td>
        <td>{{companies.name}}</td>

        <!-- 5.6 Add a link to update 
        -->
        <td><a href="/customers/{{customer_id}}/edit">Edit</a>
    </tr>
    {{/each}}
</table>
{{/block}}
{{/extends}}
